# Django Settings
DEBUG=False
SECRET_KEY=your-secret-key-here-CHANGE-THIS-IN-PRODUCTION
ALLOWED_HOSTS=your-domain.com,localhost,127.0.0.1

# Database - SQLite (para desarrollo) o PostgreSQL (para producción)
# Para SQLite (más simple):
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3

# Para PostgreSQL (descomentar si lo usas):
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=valletpvbot_webhook
# DB_USER=postgres
# DB_PASSWORD=your-database-password
# DB_HOST=localhost
# DB_PORT=5432

# Telegram Bot
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE
TELEGRAM_WEBHOOK_URL=https://your-domain.com/telegram/

# API Key para autenticación de servidores TPV (debe coincidir en todos los TPV)
TPV_API_KEY=generate-a-strong-random-key-here

# Redis (for Celery and caching)
REDIS_URL=redis://localhost:6379/0

# CORS (TPV Servers) - Ya no se usa aquí
# El CORS ahora es dinámico vía modelo AllowedOrigin en el admin de Django
# No configurar CORS_ALLOWED_ORIGINS aquí
