Me haces una vista django llamada get_estado_ventas que envie al cliente un JsonResponse(response)

Quiero todas las Lineaspedido(infmesa_id) de Infmesa que tenga Infmesa(fecha) mayor que 
el ultimo cierre en  Cierrecaja(fecha). Donde Lineaspedido tiene una columna llamada estado con valores "C", "N", "P".
quiero la suma total de Lineaspedo(count(telca_id)*precio).
con este formato:

# Crear un JSON con los resultados
    resultado = {
        "cobrado": suma_total_c, ##estado="C"
        "pedido": suma_total_p, ##estado="P"
        "borrado": suma_total_n ##estado="N"
    }
    

Me haces una vista django llamada get_estado_vetas_by_cam que envie al cliente un JsonResponse(response)

Quiero todas las Lineaspedido(infmesa_id) de Infmesa que tenga Infmesa(fecha, hora) mayor que 
el ultimo cierre en  Cierrecaja(fecha, hora) . Donde Lineaspedido tiene una columna llamada estado con valores "P".
quiero la suma total de Lineaspedo(can=count(idart), can*precio). Lineaspedido tiene una relacion con Pedidos por medio de pedido_id
y Pedidos no tiene una relacion con Camareros. Pero tiene una columna camarero_id que puede tener un camarero o no. Por medio de
estas relaciones agrupame los totales vendidos por cada camarero despues de la ultima fecha, hora del ultimo cierre.
con este formato:


fecha: String
hora: String

concat=fecha + " " +hora

# Crear un JSON con los resultados
    resultado = [
        {nombre, total_vendido}
    ]
    

Me haces una vista django llamada get_estado_ventas que envie al cliente un JsonResponse(response)

Quiero todas las Lineaspedido(infmesa_id) de Infmesa que tenga Infmesa(fecha) mayor que 
el ultimo cierre en  Cierrecaja(fecha). Donde Lineaspedido tiene una columna llamada estado con valores "C", "N", "P".
quiero la suma total de Lineaspedo(count(telca_id)*precio).
con este formato:

# Crear un JSON con los resultados
    resultado = {
        "cobrado": suma_total_c, ##estado="C"
        "pedido": suma_total_p, ##estado="P"
        "borrado": suma_total_n ##estado="N"
    }

Crea una vista Django.

Teniendo encuenta un modelo de Lineaspedido(id, idart) donde idart pertenece a Teclas(pk, nombre)-
Quiero el numero de articulos vendidos en el mes en que estamos. articulos=teclas. Agrupados por idart y estado = "C".
La fecha se tiene que coger de Infmesa(fecha) donde se asocia por medio de Lineaspedido(infmesa_id).

Devuelve: 

# Crear un JSON con los resultados
    resultado = [{
        "can": cantidad de articulos
        "nombre": nombre
    }]